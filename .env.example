# 复制为 .env 后填写真实密钥；不要提交 .env。
DEEPSEEK_API_KEY=

# 联网主搜索。留空时，项目会尝试使用 ddgs 作为备用搜索。
ANYSEARCH_API_KEY=

# 合规复核 Agent 的判卷模型（默认智谱 GLM，OpenAI 兼容接口）。
# 留空时复核调用会失败，但复核层 fail-open，不影响主流程。
GLM_API_KEY=

# 可选：复核开关（默认开启；设为 0/false/no 可关闭）
# REVIEW_AGENT_ENABLED=1
