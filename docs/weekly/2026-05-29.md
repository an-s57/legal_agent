# 2026/05/29 复盘

## ✅ 已完成

1. **向量库建库成功** — 2 本法律 PDF（消费者权益保护法 2013 修正、民法典）共 204 个文本段，用 `nomic-embed-text` 通过自定义 `OllamaEmbeddings` 类（httpx + trust_env=False）存入 FAISS

2. **main.py 添加启动块** — `python main.py` 可启动 FastAPI 服务

3. **Agent 工具调用确认可工作** — 消息链：`HumanMessage → AIMessage(call legal_rag_search) → ToolMessage(返回 PDF 原文) → AIMessage(整合回答)`。GLM-4-flash 能正确识别工具并调用

4. **SYSTEM_PROMPT 已优化** — 明确告诉模型有哪些工具可用、什么场景用什么工具

## ❌ 未解决 / 待修复

1. **API 端点返回空 body** — `requests.post` 调 `/legal/chat` 返回空响应，JSON 解析失败
2. **tools_used 返回空** — `intermediate_steps` 的 `step[0]` 是字符串，被当成了 `.tool` 属性调用
3. **PowerShell 中文乱码** — PowerShell 5.1 默认不是 UTF-8

## 🔧 关键文件

- [rag/retriever.py](../../rag/retriever.py) — 自定义 OllamaEmbeddings + FAISS
- [agent/legal_agent.py](../../agent/legal_agent.py) — LangChain 1.3 create_agent + GLM-4-flash
- [tools/legal_tools.py](../../tools/legal_tools.py) — legal_rag_search + web_legal_search
- [main.py](../../main.py) — FastAPI 入口
- [memory/case_memory.py](../../memory/case_memory.py) — 会话管理 + LLM 案情摘要

## 📋 下一步

1. 修复 API 端点返回空的问题
2. 修复 tools_used 的 `.tool` 属性错误
3. 完整走通一次 API 请求
4. 考虑升级 Ollama 服务端版本