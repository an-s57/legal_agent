# 2026/06/14 复盘

六级考完后的复盘，总结一下。

## 时间线

- **4 月中旬** — 考完蓝桥杯，意识到算法不是自己的方向，转向工程
- **5 月中旬** — 开始 Agent 项目，初步了解 LangChain、RAG、FastAPI 概念，手敲了一遍代码
- **5 月下旬** — 向量库建库成功（FAISS + Ollama），Agent 工具调用跑通，API 端点异常修复
- **6 月初** — 稍微学习面试题，用题目倒逼理解，搞懂了 ReAct 模式、Agent 决策循环、Fine-tune vs RAG、Tool Calling vs MCP
- **6 月前半月** — 冲刺 CET-6（约 2 周真投入），项目暂停
- **6.14** — 六级考完，复盘，决定每周写一篇小记用于追溯学习



## 待学习

- 嵌入模型选择标准（为什么选 nomic-embed-text 而不是其他）
- Fine-tune vs RAG 的完整对比
- 数据时效性管理
- MCP 协议实操

## 盲区复盘（6 月初模拟面试暴露的问题）

| 问题 | 状态 |
|------|------|
| 为什么项目里有两个 LLM 实例 | ✅ 已搞懂 |
| Agent 内部怎么决定调哪个工具 | ✅ 已搞懂（ReAct + 语义匹配） |
| 为什么写自定义 OllamaEmbeddings | ✅ 已搞懂（绕过 langchain_ollama 不兼容） |
| Fine-tune 是什么 | ⚠️ 概念知道，没实操 |
| MCP 是什么 | ⚠️ 概念知道，没实操 |

## 决定

每周写一篇小记，记录学到的内容和遇到的问题。面试表达需要继续练习。

## 06.14 学

### 微调和rag的对比

微调（Fine-tune）：训练模型，永久地改变模型的权重，贵，不及时。
RAG 临时地改变模型的上下文，便宜，快速。

### 为什么需要重排序？

RAG现在的检索是这样的：
用户问一个问题 → 把问题转成向量 → 在向量库里找"距离最近"的几个chunk → 直接喂给LLM
这个"距离最近"用的是向量相似度，比如余弦相似度。它衡量的是语义上像不像。
     **但"语义像"不等于"对回答这个问题有用"**。
举个例子：用户问"合同违约的赔偿标准是什么"，向量检索可能给你返回5个chunk：

chunk1：讲违约金计算方式 ✅ 真正有用
chunk2：讲违约的定义 — 语义很像，但对回答没啥用
chunk3：讲合同解除条件 — 也沾边，但不是重点
chunk4：讲赔偿的诉讼流程 — 有点用但不核心
chunk5：讲违约责任的免除情形 — 语义也像，但答非所问

向量检索把这5个都给你了，**排序也不一定对**。如果直接拼进Prompt，**LLM拿到的是一堆噪声混着有用信息，回答质量就下降了**。
重排序做的事就是：拿到这5个候选chunk之后，用一个更精准的模型重新打分，**把真正有用的排到前面，把噪声过滤掉**。
这个"更精准的模型"通常是Cross-Encoder，它不是把问题和chunk分别转向量再比距离，而是把"问题+chunk"拼在一起输入模型，让模型直接判断**这个chunk对这个问题的相关程度**。精度更高，但速度慢，所以只用来**对少量候选结果重排，不用来全量检索**。
### 独立编码 联合编码 
Transformer里有个Self-Attention机制，它能让一段文字里的每个词都去关注其他词。**比如"他打了我"，模型读"打"这个词的时候，能同时看到"他"和"我"，知道是谁打谁**。
>`向量检索 `的做法是：用嵌入模型把问题单独处理一遍，得到一个向量。再把chunk单独处理一遍，得到另一个向量。这两次处理是完全独立的，问题在被处理的时候根本不知道chunk长什么样，chunk在被处理的时候也不知道问题是什么。处理完了才放在一起算相似度。
>`Cross-Encoder`的做法是：把问题和chunk拼成一段话，比如拼成"[问题] 违约赔偿怎么算 [chunk] 违约金不得超过实际损失的130%..."，然后一起喂给模型。模型在处理"违约赔偿怎么算"这几个字的时候，通过Attention机制，能同时看到后面chunk里的内容。*反过来也一样，模型处理chunk内容的时候，也能看到前面的问题。这就是"互相看见对方"*。

所以*独立编码"就是分开处理，处理时互不知情。"联合编码"就是拼在一起处理，处理时互相参考*
结果就是：**联合编码的模型能判断"这个chunk对回答这个具体问题有没有用"**，而不只是"这个chunk和问题语义上像不像"。这两件事不一样。

### rag构建流程
1. 概要
   *RAG 分两个阶段：离线建库 + 在线检索*

2. 建库阶段
   "我用 PDFPlumberLoader 加载法律 PDF
    → RecursiveCharacterTextSplitter 按 chunk_size=1000、overlap=200 切
    → nomic-embed-text 转 768 维向量
    → 写入 FAISS，存到本地磁盘"

3. 检索阶段（30秒，说你的代码 + 思考）
   "用户提问后，先转向量
    → FAISS 相似检索，目前直接取 top 5
    → 如果知识库扩大会加一层 Rerank 做精排
    → 结果**拼接**成结构化 prompt，标注来源
    → 发给 LLM 生成回答"

4. 收（10秒）
   "目前跑通了全链路，后续优化方向是检索效率和数据评估"（没做）

### agent loop(代理循环)
我目前用的是**ReAct**（thought--action--observation--thought--。。。。）


**observation**工具返回结果，系统/langgraph给llm看
还有plan-and-Execute,Reflexion.

react是边想边做，适合*短链路任务（决策步数少）*灵活，但是没有约束，可能跑偏。
plan-and-execute先计划在执行，适合*复杂*的任务，计划可能不会实现
reflexion react之后进行*自我纠错*，准确率高，慢


### 完整流程
1.用户发送消息，类型为ChatResquest,session_id,message,fastapi接收并且检验id,message都在，async def legal_chat(req:ChatResquest)异步处理请求

2.get_session(req:session_id)跳转到case_summary，session_id第一次出现，创建{"history":[],"case_summary":{}}

3.把history转换成langchain消息格式，history=[HumanMessage(),AImessage(),,,]

4.把case_summary转换成json字符串

5.调用agent
result=await run_legal_agent(req.message,history,case_summary)

a.创建 LangGraph agent
def create_legal_agent(),langgrapg内部把llm+tools+system_prompt打包成一个状态图

b.组装messages

6.agent内部的循环
启动，上来先有system prompt，humanMessage给llm开始思考/thought，输出thought+action
系统执行工具，比如leagl_rag_search

a.load_local,加载磁盘上的向量库
b.将humanmessages（query）转向量
c.retrieve相关的5块。
d.拼上来源
返回observation,打包成toolmessages

消息列表变成[systemmessage,humanmessage,aimessage   (thought+action),toolmessage    (observation)]

llm看见observation,思考，生成回答

7.agent返回结果，{
   "output":"",
   "intermediate_steps":[]
}
最终回答+用了什么工具

8.我们又回到main.py,然后又去了case_memory.py
更新历史history


{
   "abc-1":
   {
      "history":[{"human":"拖欠工资..."},"ai":"我建议...."],
      "case_summary":{}
   }
}

更新案情摘要,update_case_summary(req.session_id,exchange)

{
   "abc-1":
   {
      "history":[{"human":"...","ai":"..."}],
      "case_summary":
      {
         "case_type":"",
         "key_facts":[],
         "user_claim":""
      }
   }
}

9.回到main.py
打包返回
return{
   "answer":"...",
   "session_id":"abc-1",
   "tool_used":["legal_rag_search"],
   "case_summary":{
      "case_type":
      "key_facts":
      "user_claim":
   }
}

当用户第二次继续询问时，
messages=[
   HumanMessage(".."),
   AIMessage(".."),
   SystemMessage("当前案情摘要：{…}"),  ← 给 Agent 注入背景
   HumanMessage("那我能拿多少赔偿？")   ← 新问题
]


### 防止幻觉机制

1、system prompt，行为约束，“我向它介绍了工具，优先用工具且标注来源。如果查不到，用自己的知识，必须标注请核实”
案情摘要
2、rag+来源标注
3、memory，messages历史

### memory机制
sessions["abc-123"] = {
    "history": [                          ← 第1层：短期记忆（原文）
        {"human": "...", "ai": "..."},
        {"human": "...", "ai": "..."},
        ...
    ],
    "case_summary": {                     ← 第2层：长期记忆（压缩）
        "case_type": "劳动纠纷",
        "key_facts": ["拖欠3个月", "未签合同"],
        "user_claim": "申请劳动仲裁"
    }
}

1、短期记忆，太多了，上下文窗口会爆，装不下。
2、案情摘要，压缩，转成SystemMessage存入messages