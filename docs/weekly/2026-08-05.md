# 0805
## 分块策略对比

做这个好处是：**chunk 策略影响检索质量**，是经典调参点。提升evidence_hit

检索的依据是**evidence_hit**

最开始我将chunk size设为500，800,1000，1500到2000，

| PDF | 页数 | 总字数 | 单页最长（字） |
|---|---|---|---|
| 消费者权益保护法 | 16 | 8312 | 681 |
| 产品质量法 | 18 | 8720 | 587 |
| 反不正当竞争法 | 13 | 6243 | 562 |
| 广告法 | 24 | 11476 | 567 |
| 电子商务法 | 22 | 10653 | 574 |
| 食品安全法 | 63 | 31960 | 605 |
| **合计** | **156** | **77,364** | **最长段落 680** |



其次我们切分的工具首先是用`分隔符`切分，其次再是`chunk size`的长度大小。所以只有chunk size等于500的时候切分成了250块,其他时候都是156块,即一页为一块，PDF本身就是天然块。

如果换`<680的chunk size`会更好一些吗？比如300,400,500，我也进行了实验但是实验证明数据并不如。因为在实验过程中有完整的法律条文被切断了，所以检索不到了。

结果：

| chunk_size:overlap | 文本段数 | 证据命中 | 说明 |
|---|---|---|---|
| 500:100 | 250 | 25/33 **(75.8%)** | 唯一触发切分 → 法条被腰斩，反而更差 |
| 800:160 | 156 | 26/33 **(78.8%)** | 每页一块 |
| 1000:200（生产默认） | 156 | 26/33 (78.8%) | 每页一块 |
| 1500:300 | 156 | 26/33 (78.8%) | 每页一块 |
| 2000:400 | 156 | 26/33 (78.8%) | 每页一块 |
| 1000:0 / 1000:100 / 1000:300 | 156 | 26/33 (78.8%) | 每页一块 |

**结论**：chunk_size ≥ 680 不触发切分，全部 = 每页一块 = 156 段（逐字节一致）；
唯一触发切分的 500 反而更差——物理行切分腰斩完整法条；
该语料下 chunk_size 不是检索质量的杠杆，7 道失败题是短锚句召回缺口，指向混合检索。

*失败题的证据锚句就在完整块里、就在正确页码上*，却进不了 Top-5。块已经是"完整法条"了，还能怎么优化块？——块不是瓶颈，**召回才是**。**短锚句（"销售者赔偿后……"这种 10~17 字的句子）在向量检索里词面匹配弱**，这是*检索侧的经典弱点*，跟 chunk_size 无关。

**overlap 只在切分发生时才有意义**——它决定相邻两块之间共享多少字。切分没发生 → 每页一块 → 没有"相邻块"可以重叠 → overlap 完全空转。



## WSL 部署   把项目从D盘迁到WSL

Windows适用于Linux的子系统在Windows里面直接跑完整的Linux环境。 WSL2内部是一个轻量化的虚拟机性能强

目标：把空白 WSL Ubuntu 变成能跑项目的环境

1.拷代码2.建环境3.验证基础4.复用模型5.启动后端

以后就都在WSL里面跑代码了

### 踩坑记录（按顺序，这是最有价值的部分）

1. 忘记 sudo 密码
   - WSL 特性：Windows 侧可以绕过密码直接进 root（这是 WSL 自带的，面试能讲）
   - 在 PowerShell 跑：wsl -d Ubuntu -u root，然后 passwd an 重置密码
   - 注意：Linux 输密码不显示任何字符，这是正常的

2. Ollama 安装报错缺 zstd
   - 报错信息本身就写了怎么治：sudo apt-get install zstd
   - 装软件通用动作：sudo apt-get update && sudo apt-get install -y 包名
   - 结论：Linux 下报错是说明书，先读报错

3. 下载巨慢（估计要 2.5 小时）
   - 根因：WSL 不走 Windows 的代理。Windows 侧代理只对 Windows 生效，WSL 是另一套系统，不经过它直连所以慢
   - 配代理的完整链路见下一条

4. 给 WSL 配代理（完整排障链路）
   - 查 Windows 真实 IP：ip route show default，看 default via 后面的 IP（<Windows-IP>）
     - 坑：resolv.conf 里的 <DNS网关IP> 只是 DNS 网关，不监听代理端口
   - 代理软件开 Allow LAN（允许局域网连接），并且必须完全退出重开才生效
   - 验证监听：netstat -ano | findstr :<端口>，Local Address 必须变成 0.0.0.0:<端口>
     - 127.0.0.1 = 只监听本机，其他机器连不进
   - 防火墙放行（管理员 PowerShell）：
     New-NetFirewallRule -DisplayName "WSL proxy" -Direction Inbound -LocalPort <端口> -Protocol TCP -Action Allow
   - 写进 ~/.bashrc 持久化，每次启动自动生效：
     export hostip=$(ip route show default | awk '{print $3}')
     export http_proxy="http://$hostip:<端口>"
     export https_proxy="http://$hostip:<端口>"
   - 用动态查 IP 而不是写死：WSL 重启后 NAT 网卡 IP 可能变，写死会失效

### 命令速查表（不用背，存着查）
| 命令 | 干什么 |
| --- | --- |
| curl -I URL | 看 URL 通不通（网络诊断第一步） |
| curl -fsSL URL \| sh | 下载脚本并执行 |
| sudo apt-get update && sudo apt-get install -y 包名 | 装软件 |
| systemctl status 服务名 | 看后台服务状态 |
| wsl -d Ubuntu -u root | 以 root 进 Ubuntu（忘密码的救命招） |
| passwd 用户名 | 改密码 |
| ip route show default | 查网关（Windows 主机 IP） |
| netstat -ano \| findstr :端口 | 看谁在监听某端口 |
| source ~/.bashrc | 让刚改的配置立即生效 |


# 0806

## 检索升级 BM25   混合检索

在纯向量检索下这个短锚句在语义空间里匹配弱。这正是BM25的强项。
进化三部曲

1. **一个词出现的次数越多越相关，但是收益是递减的，而且对短文档不公平TF**

2. **稀有词更值钱，一个词出现的越少，权重占比越高，区分度越高IDF**

3. **词频饱和长度归一化**，词频饱和就是相关度不是线性增长的，用这篇文档的长度除以含这个词的平均长度，按词频将文档拉回一个公平的尺度

score(d) = Σ  IDF(qi)  ×  f(qi,d) × (k1 + 1)
              ─────────────────────────────
              f(qi,d) + k1 × (1 - b + b × |d|/avgdl)

手算小例子

假设语料只有 3 篇文档，查询是 "撤销权"：

D1: 债务人以明显不合理的低价转让财产    （长，无"撤销权"）
D2: 撤销权行使期限为一年               （中，有 1 次）
D3: 债权人撤销权                        （短，有 1 次）
打分过程：

IDF："撤销权"出现在 D2、D3 两篇（n=2，N=3），IDF = ln(1.5/2.5)+1 ≈ 0.49——不算高，因为不够稀有
D1：f=0 → 整项为 0 → 直接淘汰（没命中就是没命中）

D2 vs D3：词频都是 1，但长度归一化因子不同——D3 短（|d| 小），(1 - b + b×|d|/avgdl) 小，分式值更大 → D3 排在 D2 前面

这就是 BM25 的典型行为：**同样命中一个词，短文档赢；没命中的文档直接没分**。

### BM25的局限

1. 同义词认不出，
2. 语序无感
3. 拼错，简繁，口语化说法都失效

### BM25过程

1. 准备
2. 搜索引擎模式**分词**，把一个词切的更细一些，过滤掉空白，最后返回列表
3. 建BM25索引，
    a. `加锁`，保证只有一个线程
    b. 双重检查`不重复建索引`
    c. 然后`BM25和向量库同源`
    d. 得到`词列表`
    e.用BM250kapi`建索引`
    f.返回索引和文档列表


## RRF

不看两路的原始分数，**因为FAISS和BM25度量的不一样，没有办法直接比，所以只看排名**。每篇文档在向量路排第几名，在BM25排第几名，折合成分数加起来。

## P3 混合检索开发集结果（33 题）

| 配置 | 证据命中 | 救回 | 新丢失 | 平均总耗时 |
|---|---|---|---|---|
| 基线 纯向量 k=40 | 26/33 (78.8%) | — | — | 3297ms |
| **k_bm25=20, 候选池=40** ? | **33/33 (100%)** | **7/7** | **无** | **3423ms** |
| k_bm25=20, 候选池=60 | 31/33 (93.9%) | 6 | r13 | 4128ms |
| k_bm25=40, 候选池=40 | 31/33 (93.9%) | 6 | r14 | 3429ms |
| k_bm25=40, 候选池=60 | 32/33 (97.0%) | 7 | r14 | 5163ms |

> 参数：k_vector=40, top_k=5, rrf_k=60。来源命中率全配置 100%。
> 基线失败题：r01 r05 r06 r16 r18 r28 r29（P1 确认的短锚句召回缺口）。

### 混合检索的结论

**混合检索把证据命中率从纯向量的 78.8% 提升到 100%（开发集 recall@5 = 33/33），验证集 17/17 复测通过。**


# 0808

## 梳理思路


用户提问
  → frontend/src 前端（React）
  → main.py  POST /legal/chat/stream（SSE）
  → memory/case_memory.py  从 SQLite 加载历史对话 + 案情摘要
  → agent/legal_agent.py   LangGraph 图：
       planner 节点（信息够不够？不够→追问）→ llm 节点（决定调什么工具）
       → tools/legal_tools.py  执行工具
            legal_rag_search → rag/retriever.py → rag/hybrid.py（FAISS+BM25+RRF）→ bge-reranker → Top5
            web_legal_search → AnySearch 主搜索 / ddgs 兜底
       → llm 节点整合回答
  → agent/hallucination_guard.py  幻觉守卫（校验引用是否在检索结果里）
  → 流式回传前端 → SQLite 存对话 → 后台更新案情摘要
## 混合检索评测（WSL 复跑）

### 为什么复跑
08-05 在 Windows 归档是 33/33（100%），但生产跑在 WSL。复跑验证“换环境结果是否一致”——结果不完全一致，差异集中在一道题 r14。

### Development 33 题（WSL）

| 配置 | 证据命中 | 平均总耗时 |
|---|---|---|
| 纯向量基线 | 25/33 (75.8%) | — |
| k_bm25=20, cand=40 | 31/33 (93.9%) | ~6.1s |
| k_bm25=40, cand=60 | 32/33 (97.0%) | ~8.7s |

最佳档仍救不回 r14（基线就失败）。其余档救回 6/7。

### Validation 17 题（WSL）

| 配置 | 证据命中 |
|---|---|
| 纯向量基线 | 13/17 (76.5%) |
| 四档混合检索 | 全部 17/17 (100%) |

基线 4 道失败题（r10 r12 r22 r48）四档任意一档全救回。验证集天花板饱和，与 08-05 一致。

### r14 漂移根因：reranker 环境抖动

调查链（逐层排除）：
1. 向量库 hash 两端逐位一致 √
2. 两端 Ollama embedding 模型 ID 一致 √
3. python httpx 实测两端 embedding 完全一致（cos=1.0）√
4. jieba / rank_bm25 版本两端一致 √
5. 唯一差异：reranker 运行环境——Windows torch 2.12.0+cpu 与 WSL torch 2.13.0+cu130，WSL 还是虚拟化 CPU，导致边缘题 rerank 浮点抖动，证据掉出 Top-5

**教训：跨 WSL 测 embedding 一致性必须用 python，别用 curl**——curl 在 Git Bash 与 WSL 之间引号转义会污染请求，误测出 cos=0.54，是假象。

### 定案
- 生产配置保持 k_bm25=20 / cand=40 不动：验证集四档无区分度（全 100%），候选池加大只多耗 25%~45% rerank 时间，无收益。
- **WSL 结果才是生产真实水平**：开发集 97%（32/33）、验证集 100%。08-05 的 33/33 是 Windows 环境满贯。
- P3 混合检索闭环。

### 收尾
- evaluation/README.md 已删（历史定案表格记录的是旧参数，结论以 results/ json 为准）
- WSL 三个新结果文件已拷回 Windows 归档（sync.sh 单向，避免下次 --delete 冲掉）
