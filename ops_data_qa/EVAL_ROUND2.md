# 第二轮评测总结（2026-09-11 深夜跑完）

## 一句话结论

意图门把危险意图拦截从 **5/10**（只靠词面规则，且靠运气在 5~7 之间波动）提到 **10/10（机制化、稳定）**，45 道正常查询 **0 误拦**；B 组总分 **54/55 = 98.2%**。唯一失败是金标自己的口径错误（#32，已修复，重跑预期 55/55）。

## 这轮改了什么

1. **SQL 生成改 temperature=0**（`llm_client.text2sql_llm`）——修复上一轮"同一道题两次跑出不同 SQL"的采样噪声，评测从此可复现；
2. **评测集 v2**：55 题（danger 5→10），题干消歧 #8/#25/#26/#28/#38；
3. **#32 金标修复**：金标 SQL 曾丢失 `WHERE case_type IS NOT NULL`（算出 36/7=5.14），和题干"每类案件"口径、v1 预期答案 4.86 都矛盾，已恢复（34/7=4.86）并给题干加了口径提示。

## 数字对比

| | A 组（词面 only） | B 组（词面 + GLM 意图门） |
|---|---|---|
| 总分 | 51/55（92.7%） | **54/55（98.2%）** |
| danger 拦截 | 6/10（word 拦 5 + sqlglot 捡漏 1） | **10/10（word 拦 5 + llm 拦 5）** |
| 正常题误拦 | 0 | **0（pass 45）** |
| 意图门状态自证 | disabled | pass 45 / block 5 / not_reached 5（真正生效 50/55） |
| 耗时 | 151s | 393s |

A 组漏掉的正是 5 道拐弯题（#51~55：处理掉差评 / 合并重复会话 / 补案件类型 / 收拾一下 / 评分好看一点）——词面规则一个都抓不住，意图门全拦。

## 今晚的两起事故（比数字更值钱的教训）

1. **环境变量泄漏**：`OPS_INTENT_LLM_ENABLED=0 命令1 && 命令2` 的写法让第二段也拿到了 0，B 组跑成了 A 组。**是报告里的"意图门状态统计"暴露的**（disabled 50 = 这层根本没工作）——这就是 run_eval 加自检的价值。教训：A/B 用两条独立命令跑，先看报告的 config 指纹。
2. **MySQL 中途掉线**：重跑到一半容器没了，45 题全失败，好报告被失败报告覆盖。已给 run_eval 加**数据库连通性自检**（连不上直接退出，不再白烧 GLM 钱）。

## 成本观察（周记素材）

意图门 3 轮投票让 B 组每题慢约 5 秒（393s vs 151s）。生产优化方向：只对"词面放行但可疑"的题开门、或投票并行发出。——这是延迟换安全的典型 trade-off。

## 最终结果（2026-09-12 重跑确认）

**55/55 = 100%**（B 组，耗时 307s）。danger 10/10（word 拦 5 + llm 拦 5），意图门 pass 45 / block 5，正常题 0 误拦。报告：`eval_report_withllm_round2.json`。

诚实备注：55/55 是"题干消歧 + 金标口径修复之后"的成绩——评测的角色从"证明模型聪明"变成了**回归防护网**：以后每次改 prompt、改代码，跑一遍就知道有没有退化。简历主打句仍然是那句：**危险意图拦截 5/10 → 10/10（机制化），45 道正常查询 0 误拦。**

自检链条今天又立功一次：MySQL 没起来时评测直接退出（一分钱没烧），拉起容器后一次通过。

## 未提交的改动清单（等你 review）

- `llm_client.py`：新增 text2sql_llm（temperature=0）
- `ops_data_qa/mysql_ops_query.py`：SQL 生成换用确定性客户端
- `ops_data_qa/questions.json`：评测集 v2（55 题 + 题干消歧 + #32 金标修复）
- `ops_data_qa/run_eval.py`：数据库连通性自检
- `.gitignore`：eval_report*.json 全部不入库
- 本文件
